%
% Keystroke-like font
%
% As per some available fonts which mimic the look of
% keys on a keyboard, except this this adjustable.
%
% More (many more, in fact) options are needed before
% this can be considered a working package
%

\NeedsTeXFormat{LaTeX2e}[1996/06/01]
\ProvidesPackage{ceystroke}[2019/11/03 Capiar Keystroke "font"]

\RequirePackage{
  tikz,
  textcomp,
}

\usetikzlibrary{
  shadows,
}

\newdimen\keyscale
\keyscale=20pt

\newdimen\keypad
\keypad=6pt

\newcommand*\keystroke[1]{%
  \tikz[baseline=(key.base)]
    \node[%
      draw,
      fill=white,
      drop shadow={shadow xshift=0.25ex,shadow yshift=-0.25ex,fill=black,opacity=0.75},
      rectangle,
      rounded corners=4pt,
      inner sep=\keypad,
      minimum height=\keyscale,
      minimum width=1.5\keyscale,
      line width=0.125\keypad,
      font=\large\ttfamily\bfseries
    ](key) {#1\strut}
  ;
}

\endinput
